import React from 'react';
import Breadcrumb from "../../components/Breadcrumb";

const Expenses = () => {
    return (
        <div>
            <Breadcrumb pageName='Expenses List' />
            Welcome to expenses page
        </div>
    );
};

export default Expenses;